// ============================================================
// TopBar.tsx 패치 가이드
// ============================================================
// 
// 파일: src/components/TopBar.tsx
//
// 변경: 관리자 설정 접근 레벨을 70(본부장) → 90(회사관리자)으로 변경
//
// 이유: 본부장은 OKR 관리/승인 등 업무 권한만 필요하고,
//       사용자 관리/역할 관리/조직 구조 설정 같은 시스템 관리는 
//       회사 관리자(레벨 90) 이상만 접근해야 함
//
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// 찾기 (약 라인 ~130 근처):
// ─────────────────────────────────────────
//
//   {/* 관리자 설정 (레벨 70 이상) - 기존 */}
//   {roleLevel >= 70 && (
//
// ─────────────────────────────────────────
// 바꾸기:
// ─────────────────────────────────────────
//
//   {/* 관리자 설정 (회사 관리자 이상, 레벨 90+) */}
//   {roleLevel >= 90 && (
//
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// 한 줄 변경입니다. 에디터에서 Ctrl+H로:
// roleLevel >= 70  →  roleLevel >= 90